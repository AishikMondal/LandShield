import { useEffect, useState } from 'react';
import { Map, BellRing, BrainCircuit, Camera, Phone } from 'lucide-react';
const sections=[{id:'gis-map',label:'Risk Map',icon:Map},{id:'alert-broadcast',label:'Alerts',icon:BellRing},{id:'ai-models',label:'Model Stack',icon:BrainCircuit},{id:'citizen-reporting',label:'Citizen Reports',icon:Camera},{id:'emergency',label:'Emergency',icon:Phone}];
export function ModelNavBar(){const[visible,setVisible]=useState(false);useEffect(()=>{const fn=()=>setVisible(window.scrollY>500);window.addEventListener('scroll',fn,{passive:true});return()=>window.removeEventListener('scroll',fn)},[]);if(!visible)return null;return <div className="fixed top-16 left-0 right-0 z-40 bg-[#0B0F17]/95 backdrop-blur-lg border-b border-[#1E293B]"><div className="max-w-7xl mx-auto px-4 lg:px-6"><div className="flex items-center gap-1 h-12 overflow-x-auto"><span className="text-[10px] text-slate-500 font-semibold uppercase tracking-wider mr-2 hidden md:inline">Command:</span>{sections.map(s=>{const Icon=s.icon;return <button key={s.id} onClick={()=>document.querySelector(`#${s.id}`)?.scrollIntoView({behavior:'smooth'})} className="flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium whitespace-nowrap border border-transparent text-slate-400 hover:text-slate-200 hover:bg-slate-800/40"><Icon className="w-3.5 h-3.5" />{s.label}</button>})}</div></div></div>}
