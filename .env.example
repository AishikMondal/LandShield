# Frontend API base URL (used by src/api/client.ts).
VITE_API_BASE_URL=http://localhost:8000

# When demonstrating live alerts over the LAN, point another machine at the
# backend host instead, e.g.:
#   VITE_API_BASE_URL=http://192.168.1.10:8000